/*
			fprintf(stderr, "%-12s %6.2f %5.1f", stnm, delta, azim);
			if (Pmin != FLT_UNDEF) {
				fprintf(stderr, "    %2d %2d%c%4d", Pmin, Psec, Ppol, Pres);
			} else {
				fprintf(stderr, "              ");
			}
			if (Smin != FLT_UNDEF) {
				fprintf(stderr, "   %2d %2d%4d", Smin, Ssec, Sres);
			} else {
				fprintf(stderr, "            ");
			}
			if (pPmin != FLT_UNDEF) {
				fprintf(stderr, "   %2d %2d", pPmin, pPsec);
			} else {
				fprintf(stderr, "        ");
			}
			if (Suppmin != FLT_UNDEF) {
				fprintf(stderr, "   %2d %2d", Suppmin, Suppsec);
			} else {
				fprintf(stderr, "        ");
			}
			if (strcmp(Supp, CHAR_UNDEF) != 0)
				fprintf(stderr, " %s", Supp);
			fprintf(stderr, "\n");
*/
